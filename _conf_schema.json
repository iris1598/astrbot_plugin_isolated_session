{
  "memory_groups": {
    "type": "template_list",
    "description": "启用记忆的群聊列表。配合官方「会话隔离(unique_session)」时，每位成员的记忆相互独立。",
    "hint": "需先在 WebUI 平台设置中开启 会话隔离(unique_session)，否则群聊内所有成员共享同一份记忆。本列表为空时自动兼容读取旧插件配置结构 whitelist_groups 中的 memory_enabled 字段",
    "templates": {
      "group_config": {
        "label": "群聊配置",
        "items": {
          "group_id": {
            "type": "string",
            "description": "群聊 ID",
            "default": "",
            "hint": "填写群聊 ID（官方会话隔离开启后即为群号/session ID 中的群部分）"
          },
          "group_name": {
            "type": "string",
            "description": "群聊备注名（可选）",
            "default": ""
          },
          "memory_enabled": {
            "type": "bool",
            "description": "该群聊是否启用成员记忆",
            "default": true,
            "hint": "关闭后该群所有成员不再召回与抽取；需全局 memory_enabled 开启"
          }
        }
      }
    },
    "default": []
  },
  "enable_debug_log": {
    "type": "bool",
    "description": "是否启用调试日志",
    "default": false
  },
  "favorability_reset_eval_with_session": {
    "type": "bool",
    "description": "/会话重置 时是否同步清除当前人格的好感度评价（需安装 astrbot_plugin_favorability）",
    "default": true,
    "hint": "若已安装 astrbot_plugin_favorability 插件，开启后在会话重置时将同步重置当前人格对该用户的评价（印象重置为初次见面），严格保留好感度数值、关系档位、禁言等其它所有数据。"
  },
  "compress_provider_id": {
    "type": "string",
    "description": "/会话压缩 专用 LLM 模型（可选）",
    "default": "",
    "hint": "留空则使用当前会话的聊天模型进行压缩。填写 Provider 的完整 ID",
    "_special": "select_provider"
  },
  "compress_timeout": {
    "type": "int",
    "description": "/会话压缩 LLM 请求超时时间（秒），0=不限制",
    "default": 30,
    "hint": "超时将直接报告压缩失败且不修改上下文，建议 10-60"
  },
  "compress_instruction": {
    "type": "text",
    "description": "/会话压缩 自定义提示词（留空使用默认）",
    "default": ""
  },
  "memory": {
    "type": "object",
    "description": "记忆系统（基于共享知识库的随时间衰减记忆）",
    "hint": "记忆按 会话归属 owner（官方会话隔离下即 群×用户）隔离共存于所选共享知识库。相关度 × 时效衰减排序注入；被召回的会刷新时间戳（强化），超过遗忘阈值自动清扫删除。",
    "items": {
      "memory_enabled": {
        "type": "bool",
        "description": "记忆系统总开关",
        "default": false,
        "hint": "需先在 WebUI 创建知识库（配置 Embedding 模型），并在下方 memory_kb_name 中选择"
      },
      "memory_kb_name": {
        "type": "list",
        "items": {
          "type": "string"
        },
        "description": "共享记忆知识库（单选）",
        "default": [],
        "_special": "select_knowledgebase",
        "hint": "选择用户自建的一个知识库。所有用户的记忆以带 owner 元数据的 chunk 共存于该库，按会话归属隔离。请勿删除该库，否则记忆将丢失"
      },
      "memory_extract_interval": {
        "type": "int",
        "description": "每多少轮对话触发一次记忆提取",
        "default": 3,
        "obvious_hint": true,
        "hint": "每 N 轮对话抽取一次记忆，避免每条消息都触发（消耗 LLM 调用）。触发时把间隔内积累的全部对话轮次一并交给提取 LLM。默认 3；0 = 每轮都抽取（不推荐）"
      },
      "memory_extract_provider_id": {
        "type": "string",
        "description": "记忆提取专用 LLM 模型（独立配置）",
        "default": "",
        "hint": "留空则使用当前会话的聊天模型进行提取。建议配置独立的快速模型；提取在后台异步执行，不阻塞对话回复",
        "_special": "select_provider"
      },
      "memory_extract_timeout": {
        "type": "int",
        "description": "记忆提取请求超时时间（秒），0=不限制",
        "default": 30,
        "hint": "提取在后台执行，超时仅跳过本轮提取，不影响对话"
      },
      "memory_extract_include_persona": {
        "type": "bool",
        "description": "将当前人设（Persona）提供给记忆提取 LLM",
        "default": true,
        "hint": "开启后，提取提示词会附上当前会话生效的人设文本，使提取的记忆与人设对齐"
      },
      "memory_extract_use_names": {
        "type": "bool",
        "description": "提取提示词中使用用户昵称和机器人名（替代「用户/助手」）",
        "default": true,
        "hint": "开启后，抽取提示词用消息发送者的昵称称呼用户；机器人名取下方 memory_extract_bot_name（留空用「助手」）。获取不到昵称时自动回退「用户」"
      },
      "memory_extract_bot_name": {
        "type": "string",
        "description": "提取提示词中机器人的称呼（留空用「助手」）",
        "default": "",
        "hint": "仅在 memory_extract_use_names 开启且能获取到用户昵称时生效"
      },
      "memory_extract_persona_max_chars": {
        "type": "int",
        "description": "注入提取提示词的人设文本最大字符数",
        "default": 1000
      },
      "memory_half_life_days": {
        "type": "float",
        "description": "记忆衰减半衰期（天）",
        "default": 30,
        "hint": "effective = 相似度 × 0.5^(天数/半衰期)。半衰期越短，旧记忆衰减越快"
      },
      "memory_ttl_days": {
        "type": "float",
        "description": "记忆遗忘阈值 TTL（天），超过后被清扫删除",
        "default": 90,
        "hint": "超过该天数的记忆不再召回，并在惰性清扫时从知识库中删除"
      },
      "memory_inject_top_k": {
        "type": "int",
        "description": "每次请求注入的记忆条数",
        "default": 3
      },
      "memory_min_score": {
        "type": "float",
        "description": "记忆注入的最低有效分数（0 表示不限制）",
        "default": 0.0
      },
      "memory_inject_max_chars": {
        "type": "int",
        "description": "注入记忆文本总字符上限",
        "default": 600
      },
      "memory_fetch_k": {
        "type": "int",
        "description": "记忆检索池大小（稠密/稀疏召回上限）",
        "default": 200,
        "hint": "实际池大小取 max(fetch_k, 每用户记忆上限+20)，保证 owner 过滤不漏召回"
      },
      "memory_dup_threshold": {
        "type": "float",
        "description": "记忆去重相似度阈值",
        "default": 0.9,
        "hint": "新记忆与现有记忆相似度 ≥ 阈值时，仅强化现有记忆（刷新时间戳）而不重复写入"
      },
      "memory_max_docs_per_user": {
        "type": "int",
        "description": "每用户记忆条数上限（超出按最近使用裁剪）",
        "default": 200
      },
      "memory_sweep_interval_minutes": {
        "type": "int",
        "description": "记忆惰性清扫间隔（分钟）",
        "default": 60,
        "hint": "清扫删除超过 TTL 的记忆并按上限裁剪；0=每次请求都清扫"
      },
      "memory_consolidate_enabled": {
        "type": "bool",
        "description": "遗忘前巩固（可选）：过期记忆先由 LLM 折叠为长期摘要再删除",
        "default": false,
        "hint": "开启后清扫时会额外消耗一次 LLM 调用"
      },
      "memory_reset_with_session": {
        "type": "bool",
        "description": "/会话重置 时是否一并清空该用户记忆",
        "default": false,
        "hint": "默认关闭：长时记忆独立于对话上下文；开启后重置会话会同步清除记忆（需该群在记忆启用列表中）"
      },
      "memory_mbti_enabled": {
        "type": "bool",
        "description": "MBTI 测评报告功能（/xxti /记忆测评）",
        "default": true,
        "hint": "开启后成员可用 /xxti 或 /记忆测评 依据自己已保存的全部记忆生成 MBTI 推测报告。报告只读记忆、不写回"
      },
      "memory_mbti_method": {
        "type": "string",
        "description": "报告生成方法",
        "default": "anchor",
        "options": ["anchor", "llm"],
        "hint": "anchor=嵌入锚点比对（默认）：用记忆向量与各极锚点句算余弦，纯算术，同一批记忆必定得到同一结果，且每个维度都能追溯到具体记忆；llm=交给大模型推测，表达自然但每次结果会有波动"
      },
      "memory_mbti_anchor_threshold": {
        "type": "float",
        "description": "锚点中性判定阈值（仅 anchor 方法）",
        "default": 0.02,
        "hint": "两极锚点相似度差值低于该值的记忆视为中性、不参与判定。不同 embedding 模型的相似度尺度不同：若报告里几乎全部记忆都被判为中性就调低，若判定明显是噪声就调高"
      },
      "memory_mbti_provider_id": {
        "type": "string",
        "description": "MBTI 报告专用 LLM 模型（仅 llm 方法）",
        "default": "",
        "hint": "留空则使用当前会话的聊天模型。填写 Provider 的完整 ID",
        "_special": "select_provider"
      },
      "memory_mbti_timeout": {
        "type": "int",
        "description": "MBTI 报告请求超时时间（秒，仅 llm 方法），0=不限制",
        "default": 60,
        "hint": "报告输出较长，建议 30-120。超时按失败处理，不写回任何内容"
      },
      "memory_mbti_min_memories": {
        "type": "int",
        "description": "生成报告所需的最少记忆条数",
        "default": 8,
        "hint": "记忆过少时推测没有意义，低于该条数直接提示用户继续积累记忆"
      },
      "memory_mbti_max_chars": {
        "type": "int",
        "description": "参与报告的记忆文本字符上限",
        "default": 3000,
        "hint": "按最近使用时间倒序截取，超出部分不参与分析（下限 500）"
      },
      "memory_mbti_instruction": {
        "type": "text",
        "description": "MBTI 报告自定义提示词（仅 llm 方法）",
        "default": "",
        "hint": "只替换「任务」段落；防注入声明、分析维度与 JSON 输出协议始终保留，以免报告无法解析"
      },
      "memory_mbti_render_mode": {
        "type": "string",
        "description": "MBTI 报告呈现形式（图片海报 / 纯文本）",
        "default": "image",
        "options": ["image", "text"],
        "hint": "image=基于 Pillow 原生渲染精美紧凑海报卡片（无需浏览器依赖，含八极雷达图+双极平衡图+可能人格+极简画像，不泄露记忆证据）；text=纯文本模式；图片渲染失败时自动回退为纯文本"
      }
    }
  }
}
